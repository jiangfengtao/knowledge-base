{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "node scripts/setup-prisma.mjs && prisma generate && prisma db push --accept-data-loss && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["hkg1"],
  "github": {
    "silent": true
  }
}
